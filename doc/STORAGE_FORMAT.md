Each project entry is stored in the following format:

```JSON
{
    "id": "$int", 
    "name": "$PROJECT_NAME"
}
```

Where `id` is an auto assigned integer value assigned by gtd-rust, and `name` is how the project appears within taskwarrior.